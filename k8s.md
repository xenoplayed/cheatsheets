# Kubernetes

## kubectl

```bash
kubectl api-resources --namespaced=true # show all objects with a namespace
kubectl api-resources --namespaced=fals # show all objects without a namespace
kubectl config view --minify # Show merged kubeconfig settings used by current context 
```
